# iOS开发快速参考

## 🚀 快速命令

### 运行应用
```bash
cd "/Users/davidpony/iCloud Drive (Archive)/Desktop/工程文件夹/Bitcoin_Mining_Master/mobile_client/bitcoin_mining_master"

# iPhone (Release)
flutter run -d 00008101-001958401A30001E --release

# iPhone (Debug)
flutter run -d 00008101-001958401A30001E

# 模拟器
flutter run -d 778C78E0-171C-4BA0-9F53-A1AD5E1ED992

# 使用部署脚本（推荐）
./deploy-ios-release.sh
```

### 开发工具
```bash
# 交互式开发菜单
./ios-dev-menu.sh

# 测试应用
./test-ios-app.sh
```

### 清理和重构建
```bash
flutter clean
flutter pub get
cd ios && rm -rf Pods Podfile.lock && pod install
```

### 查看日志
```bash
# Flutter运行日志
tail -f /tmp/flutter_ios_run.log

# 后端日志
cd ../../../backend && pm2 logs bitcoin-backend

# Xcode构建日志
tail -f /tmp/xcode_build.log
```

### 测试后端API
```bash
# 健康检查
curl http://47.79.232.189/api/health

# 设备登录
curl -X POST http://47.79.232.189/api/auth/device-login \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"test-device"}'
```

## 📂 重要文件路径

```
项目根目录:
/Users/davidpony/iCloud Drive (Archive)/Desktop/工程文件夹/Bitcoin_Mining_Master

移动端:
/mobile_client/bitcoin_mining_master/

后端:
/backend/
```

## 🔧 常见问题解决

### 构建失败
```bash
flutter clean
rm -rf ios/Pods ios/Podfile.lock
flutter pub get
cd ios && pod install
```

### CocoaPods问题
```bash
cd ios
pod deintegrate
rm Podfile.lock
pod install
```

### 设备找不到
```bash
flutter devices
# 检查iPhone是否解锁
# 检查信任此电脑
```

### 证书问题
在iPhone上：设置 → 通用 → VPN与设备管理 → 信任开发者证书

## 📱 设备信息

**iPhone真机**
- ID: `00008101-001958401A30001E`
- iOS: 26.3
- 连接: 无线

**模拟器**
- ID: `778C78E0-171C-4BA0-9F53-A1AD5E1ED992`
- iOS: 26.2

## 🌐 API配置

**生产环境**: `http://47.79.232.189/api`

在 `lib/constants/app_constants.dart` 中配置

## 🎯 热键（Flutter运行时）

```
h - 帮助
r - 热重载
R - 热重启
q - 退出
c - 清屏
```

## 📊 性能指标

- 构建时间: ~22秒
- 安装时间: ~5秒
- API响应: ~0.7秒
