import Flutter
import UIKit

/// UIScene lifecycle delegate — required for iOS 26+ (UIScene API)
/// Extends FlutterSceneDelegate so Flutter's engine manages the window lifecycle.
@objc(SceneDelegate)
class SceneDelegate: FlutterSceneDelegate {
}
